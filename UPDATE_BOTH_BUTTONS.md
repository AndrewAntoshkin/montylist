# ✅ Обновление: Обе кнопки открывают форму деталей фильма

## Что изменено

Теперь **обе кнопки** открывают форму "Данные фильма" перед загрузкой видео:
- ✅ **"Новый лист"** → Форма деталей → Обычная загрузка видео
- ✅ **"Длинное видео"** → Форма деталей → Загрузка длинного видео (с разбивкой на чанки)

## Логика работы

### Вариант 1: Кнопка "Новый лист"
```
Клик "Новый лист" 
  → Форма "Данные фильма"
    → "Продолжить" → UploadModal (обычная загрузка)
    → "Пропустить" → UploadModal (без метаданных)
    → "Отмена" → Закрыть
```

### Вариант 2: Кнопка "Длинное видео"
```
Клик "Длинное видео"
  → Форма "Данные фильма"
    → "Продолжить" → UploadModalLong (chunked загрузка)
    → "Пропустить" → UploadModalLong (без метаданных)
    → "Отмена" → Закрыть
```

## Измененные файлы

### `components/TwoStepUploadModal.tsx`
- ✅ Добавлен параметр `isLongVideo` (boolean)
- ✅ В зависимости от параметра показывает `UploadModal` или `UploadModalLong`

### `components/UploadModalLong.tsx`
- ✅ Добавлена поддержка `filmMetadata` (как в обычном UploadModal)
- ✅ Метаданные передаются в API при загрузке

### `components/DashboardClient.tsx`
- ✅ Объединены состояния модальных окон в один `uploadModalState`
- ✅ Обе кнопки теперь открывают `TwoStepUploadModal` с разными параметрами
- ✅ Убран прямой импорт `UploadModalLong`

## Результат

Теперь пользователь **всегда** видит форму с деталями фильма, независимо от типа загрузки:
1. Может заполнить все поля → метаданные сохранятся в Excel и Word
2. Может нажать "Пропустить" → загрузка без метаданных
3. Может нажать "Отмена" → закрыть форму

## Тестирование

1. Нажмите "Новый лист"
   - ✅ Должна открыться форма "Данные фильма"
   - ✅ После "Продолжить" → обычная форма загрузки
   
2. Нажмите "Длинное видео"
   - ✅ Должна открыться форма "Данные фильма"
   - ✅ После "Продолжить" → форма загрузки длинного видео (с прогресс-баром чанков)

3. Заполните форму и загрузите видео
   - ✅ Метаданные должны попасть в Excel
   - ✅ Метаданные должны попасть в Word

## Что не изменилось

- ❌ Миграция БД - она уже была создана ранее (`add-film-metadata-column.sql`)
- ❌ API экспорта - уже обновлены для поддержки метаданных
- ❌ Дизайн формы - остался пиксель-перфект

## 🎉 Готово!

Обе кнопки теперь работают одинаково - сначала форма деталей, потом загрузка!




