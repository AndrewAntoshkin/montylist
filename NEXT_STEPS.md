# ⚡ СЛЕДУЮЩИЕ ШАГИ - Деплой на montylist.ru

## 🎯 ЧТО УЖЕ СДЕЛАНО:

✅ **Email полностью готов:**
- Изображения загружены в Supabase Storage
- Email template создан (`confirmation-email-supabase.html`)
- Уже можно использовать!

✅ **Код готов к продакшену:**
- Страницы входа/регистрации по дизайну
- Обработка видео по частям (20 мин)
- Экспорт в Excel и Word
- Все баги исправлены

✅ **Инфраструктура:**
- Домен montylist.ru куплен
- Supabase оплачен
- Хостинг на reg.ru есть

---

## 🚀 ЧТО ДЕЛАТЬ ДАЛЬШЕ (выберите один путь):

### 📍 ПУТЬ A: Railway (Проще и быстрее) - 10 минут

**Рекомендую этот вариант!**

#### 1. Закоммитьте код:
```bash
cd /Users/andrewaitken/carete-montage
git add .
git commit -m "Production ready: email, deployment configs"
git push origin main
```

#### 2. Деплой на Railway:
```bash
# Установите CLI
npm install -g @railway/cli

# Войдите (откроется браузер)
railway login

# Создайте проект
railway init

# Деплой
railway up
```

#### 3. Добавьте переменные:

Откройте `.env.local` и скопируйте значения:
```bash
cat .env.local
```

В Railway CLI:
```bash
railway variables set NEXT_PUBLIC_SUPABASE_URL="значение-из-env"
railway variables set NEXT_PUBLIC_SUPABASE_ANON_KEY="значение-из-env"
railway variables set SUPABASE_SERVICE_ROLE_KEY="значение-из-env"
railway variables set REPLICATE_API_TOKEN="значение-из-env"
```

#### 4. Подключите домен:

**В Railway Dashboard:**
- Откройте проект → **Settings** → **Domains**
- **Custom Domain** → введите `montylist.ru`
- Railway покажет DNS инструкции

**В reg.ru:**
- Домены → montylist.ru → **Управление DNS**
- Добавьте записи которые показал Railway (обычно CNAME)

#### 5. Дождитесь DNS (5-30 минут)

Проверка:
```bash
nslookup montylist.ru
```

#### 6. Обновите Supabase:

**Supabase Dashboard → Authentication → URL Configuration:**
- Site URL: `https://montylist.ru`
- Redirect URLs: `https://montylist.ru/auth/callback`

✅ **Готово! Сайт работает на montylist.ru**

---

### 📍 ПУТЬ B: VPS на reg.ru (Сложнее) - 1-2 часа

**Только если у вас VPS с SSH доступом!**

#### Проверьте тип хостинга:

В reg.ru → Хостинг Host-0 → есть ли **SSH доступ**?

**Если ДА** - следуйте `DEPLOY_REG_RU.md` (Вариант 1)

**Если НЕТ** - используйте Путь A (Railway)

---

## 🎯 МОЯ РЕКОМЕНДАЦИЯ:

### Используйте Railway (Путь A) потому что:

1. ✅ **Быстрее** - 10 минут vs 1-2 часа
2. ✅ **Проще** - не нужно настраивать Nginx, SSL, PM2
3. ✅ **Надежнее** - автоматический мониторинг и перезапуск
4. ✅ **Автодеплой** - git push = автоматический deploy
5. ✅ **SSL автоматический** - не нужен Certbot
6. ✅ **Цена примерно такая же** - ~500₽/мес

Настройка VPS вручную - это круто для обучения, но Railway сэкономит вам время! 🚀

---

## ⏭️ ПРЯМО СЕЙЧАС:

1. **Коммит кода** (1 мин):
```bash
git add .
git commit -m "Production ready"
git push origin main
```

2. **Откройте инструкцию:**
   - Простой путь: `QUICK_DEPLOY.md`
   - Детальный: `FINAL_CHECKLIST.md`

3. **Следуйте шагам** - через 15 минут сайт будет на montylist.ru! 🎉

---

## 🎊 Email уже работает!

**Можете прямо сейчас протестировать:**

1. Запустите локально: `npm run dev`
2. Зарегистрируйтесь на localhost:3000
3. Проверьте email - картинки уже будут работать!

Изображения грузятся из Supabase Storage - **без деплоя!** ✅

---

## 📞 Готовы к деплою?

Скажите что выбрали:
- **Путь A (Railway)** - я дам команды для быстрого старта
- **Путь B (VPS)** - дам SSH команды для настройки

Или просто запустите:
```bash
railway login && railway init && railway up
```

И через 5 минут получите рабочий URL! 🚀

