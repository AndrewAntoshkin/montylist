# 🔧 Как добавить CORS в Supabase Dashboard

## 📍 ГДЕ ИСКАТЬ CORS НАСТРОЙКИ:

### Вариант 1: Project Settings → API

1. В левом меню найди **шестерёнку** (Settings) внизу
2. Кликни на **Project Settings**
3. Слева выбери **API**
4. Найди раздел **"CORS Configuration"** или **"Allowed Origins"**
5. Добавь: `http://localhost:3000`

### Вариант 2: Authentication → URL Configuration

1. **Authentication** (слева в меню)
2. **URL Configuration**
3. **Site URL** и **Redirect URLs**
4. Там может быть поле для allowed origins

### Вариант 3: CORS настраивается через RLS Policies

**Важно:** В новых версиях Supabase CORS для Storage **автоматический**!

Если ты на **Pro плане**, CORS должен работать для всех origins по умолчанию.

---

## 🎯 ЧТО ДЕЙСТВИТЕЛЬНО ВАЖНО:

### Supabase AI сказал проверить две вещи:

1. ✅ **Signed URL expires** - МЫ УЖЕ ИСПРАВИЛИ!
   ```
   expiresIn: 1800 // 30 минут (было: 60 секунд по умолчанию)
   ```

2. ✅ **CORS на localhost** - Обычно работает автоматически

---

## 💡 СКОРЕЕ ВСЕГО CORS УЖЕ НАСТРОЕН ПРАВИЛЬНО!

Проблема была в **Signed URL expiry (60 секунд)**!

Мы это исправили → увеличили до 30 минут.

---

## 🧪 ПОПРОБУЙ СЕЙЧАС БЕЗ ИЗМЕНЕНИЯ CORS:

1. **Перезагрузи страницу:**
   ```
   Cmd + Shift + R
   ```

2. **Попробуй загрузить видео**

3. **Должно работать!** Потому что:
   - ✅ Signed URL теперь живёт 30 минут
   - ✅ Chunk size уменьшен до 10 минут (75 MB)
   - ✅ Retry логика везде

---

## 📝 ЕСЛИ ВСЁ РАВНО НЕ РАБОТАЕТ:

Тогда добавь `localhost:3000` в:

**Project Settings → API → CORS**

Но скорее всего это **не нужно** - проблема была в expiry!

---

**ПОПРОБУЙ ЗАГРУЗИТЬ ВИДЕО СЕЙЧАС!** 🚀

Signed URL теперь живёт 30 минут - должно хватить! ✅



