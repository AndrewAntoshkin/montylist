# 🚀 Деплой на reg.ru хостинг

## Важно: Проверьте тип хостинга

На скриншоте видно **"Хостинг Host-0"**. В reg.ru два типа:

---

## Вариант 1: VPS/VDS хостинг ✅

**Если у вас VPS/VDS** (полный доступ к серверу):

### ✅ Отлично! Next.js будет работать

### Шаги для деплоя на VPS reg.ru:

#### 1. Подключитесь по SSH

```bash
# Найдите SSH данные в личном кабинете reg.ru
ssh root@ваш-ip-адрес

# Или если дан логин
ssh username@ваш-ip-адрес
```

#### 2. Установите необходимое ПО

```bash
# Обновите систему
apt update && apt upgrade -y

# Установите Node.js 20
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# Установите PM2 для управления процессом
npm install -g pm2

# Установите Nginx для проксирования
apt install -y nginx

# Установите FFmpeg для обработки видео
apt install -y ffmpeg

# Проверьте установку
node --version  # Должно быть v20.x
npm --version
ffmpeg -version
```

#### 3. Загрузите код

```bash
# Установите git
apt install -y git

# Клонируйте репозиторий
cd /var/www
git clone https://github.com/ваш-username/carete-montage.git
cd carete-montage

# Установите зависимости
npm install
```

#### 4. Создайте .env файл

```bash
nano .env.local
```

Вставьте (скопируйте из вашего локального .env.local):
```
NEXT_PUBLIC_SUPABASE_URL=https://goykmdyodqhptkzfgumq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-ключ
SUPABASE_SERVICE_ROLE_KEY=ваш-service-ключ
REPLICATE_API_TOKEN=ваш-токен
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

#### 5. Соберите проект

```bash
npm run build
```

#### 6. Запустите через PM2

```bash
# Запустите приложение
pm2 start npm --name "monty" -- start

# Автозапуск при перезагрузке сервера
pm2 startup
pm2 save

# Проверьте статус
pm2 status
pm2 logs monty
```

Приложение запустится на порту 3000.

#### 7. Настройте Nginx

```bash
nano /etc/nginx/sites-available/montylist
```

Вставьте:
```nginx
server {
    listen 80;
    server_name montylist.ru www.montylist.ru;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Увеличиваем таймауты для обработки видео
        proxy_connect_timeout 600;
        proxy_send_timeout 600;
        proxy_read_timeout 600;
        send_timeout 600;
    }
    
    # Увеличиваем максимальный размер для загрузки видео
    client_max_body_size 100M;
}
```

Активируйте:
```bash
ln -s /etc/nginx/sites-available/montylist /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

#### 8. Настройте HTTPS

```bash
# Установите Certbot
apt install -y certbot python3-certbot-nginx

# Получите SSL сертификат
certbot --nginx -d montylist.ru -d www.montylist.ru

# Certbot автоматически настроит HTTPS
```

#### 9. Настройте DNS в reg.ru

В личном кабинете reg.ru:

1. **montylist.ru** → **DNS-серверы и управление зоной**
2. Добавьте A записи:
```
Тип: A
Имя: @
Значение: IP-адрес вашего VPS

Тип: A
Имя: www
Значение: IP-адрес вашего VPS
```

✅ **Готово!** Сайт доступен на https://montylist.ru

---

## Вариант 2: Виртуальный хостинг (обычный) ❌

**Если у вас обычный виртуальный хостинг** (только PHP/HTML):

### ❌ Next.js НЕ будет работать

Next.js требует Node.js сервер, а виртуальный хостинг обычно только PHP.

**Что делать:**

### Вариант A: Апгрейд на VPS

В reg.ru закажите **VPS** вместо виртуального хостинга:
- Стоимость: от 199₽/месяц
- Полный контроль
- Next.js будет работать

### Вариант B: Используйте Railway + reg.ru домен

1. **Railway** для хостинга приложения (~$5/мес)
2. **reg.ru** только для домена
3. В Railway подключите домен montylist.ru
4. В reg.ru настройте DNS на Railway

**Это дешевле и проще чем настраивать VPS!**

---

## 🔍 Как узнать какой у вас хостинг?

В личном кабинете reg.ru:

### Если это VPS/VDS:
- ✅ Будет кнопка **"Панель управления"** или **"SSH доступ"**
- ✅ Указан IP адрес
- ✅ Есть root доступ

→ **Следуйте инструкциям "Вариант 1" выше**

### Если это виртуальный хостинг:
- ❌ Только FTP доступ
- ❌ Только PHP/MySQL
- ❌ Нет SSH

→ **Используйте Railway (Вариант B)**

---

## 🎯 Рекомендация:

### Самый простой способ:

1. **Email изображения** - уже в Supabase Storage ✅
2. **Приложение** - deploy на Railway ($5 trial)
3. **Домен** - подключите montylist.ru к Railway

**Преимущества:**
- ✅ Настройка за 10 минут
- ✅ Автоматический деплой при git push
- ✅ Автоматический SSL
- ✅ Масштабирование
- ✅ Не нужно настраивать сервер вручную

**Стоимость:**
- Railway: ~$5/мес (~500₽)
- Домен reg.ru: уже оплачен

---

## 📋 Следующие шаги:

### Проверьте какой тип хостинга:

1. Зайдите в личный кабинет reg.ru
2. **Хостинги** → **Host-0**
3. Посмотрите есть ли:
   - SSH доступ?
   - Root права?
   - IP адрес?

### Если VPS:
→ Используйте инструкцию выше (Вариант 1)

### Если обычный хостинг:
→ Деплойте на Railway и подключите домен:

```bash
npm install -g @railway/cli
railway login
railway init
railway up
# Затем в Railway UI подключите montylist.ru
```

---

## 🆘 Нужна помощь?

Скажите какой тип хостинга у вас (VPS или виртуальный) и я дам точную инструкцию! 

Или просто используйте Railway - это проще и дешевле чем настраивать VPS вручную! 🚀

