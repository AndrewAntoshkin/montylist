# 🚀 Quick Start Guide

## Шаг 1: Настройка Supabase (5 минут)

### 1.1 Примените миграцию базы данных

1. Откройте Supabase Dashboard: https://goykmdyodqhptkzfgumq.supabase.co
2. Перейдите в **SQL Editor**
3. Нажмите "New query"
4. Скопируйте весь код из файла `supabase-migration.sql`
5. Вставьте в редактор и нажмите **Run**

✅ Вы должны увидеть сообщение "Success. No rows returned"

### 1.2 Создайте Storage Bucket

1. В Supabase Dashboard перейдите в **Storage**
2. Нажмите **New bucket**
3. Введите имя: `videos`
4. **Public bucket**: OFF (оставьте приватным)
5. Нажмите **Create bucket**

### 1.3 Настройте Storage Policies

1. В SQL Editor выполните этот запрос:

```sql
-- Allow authenticated users to upload videos
CREATE POLICY "Users can upload own videos"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (bucket_id = 'videos' AND (storage.foldername(name))[1] = auth.uid()::text);

-- Allow users to read their own videos
CREATE POLICY "Users can read own videos"
ON storage.objects FOR SELECT
TO authenticated
USING (bucket_id = 'videos' AND (storage.foldername(name))[1] = auth.uid()::text);

-- Allow users to delete their own videos
CREATE POLICY "Users can delete own videos"
ON storage.objects FOR DELETE
TO authenticated
USING (bucket_id = 'videos' AND (storage.foldername(name))[1] = auth.uid()::text);
```

### 1.4 Проверьте Email настройки

1. Перейдите в **Authentication** → **Providers**
2. Убедитесь, что **Email** включен
3. В **Settings** → **API** → **Site URL** укажите: `http://localhost:3000`
4. В **Redirect URLs** добавьте: `http://localhost:3000/auth/callback`

## Шаг 2: Запуск приложения (1 минута)

```bash
# Запустите dev сервер
npm run dev
```

Приложение запустится на http://localhost:3000

## Шаг 3: Тестирование (5 минут)

### 3.1 Зарегистрируйтесь

1. Откройте http://localhost:3000
2. Вы будете перенаправлены на `/auth/login`
3. Переключитесь на "Регистрация"
4. Введите email и пароль
5. Проверьте почту и подтвердите email

**Примечание**: В dev режиме Supabase может показывать ссылку подтверждения прямо в консоли.

### 3.2 Загрузите тестовое видео

1. После входа нажмите "Новый лист"
2. Загрузите короткое видео (рекомендуется до 1 минуты для быстрого теста)
3. Нажмите "Продолжить"
4. Подождите завершения обработки (это займет 1-3 минуты)

### 3.3 Просмотрите результат

1. Когда статус изменится на "Загружен", кликните на карточку
2. Вы увидите таблицу с монтажным листом
3. Нажмите "Скачать excel" чтобы экспортировать

## 🎉 Готово!

Теперь вы можете:

- ✅ Загружать видео
- ✅ Получать автоматические монтажные листы
- ✅ Просматривать таблицы с планами и диалогами
- ✅ Экспортировать в Excel

## 🔧 Что-то не работает?

### Видео не обрабатывается

**Проверьте:**
1. Replicate API token в `.env.local`
2. Логи в терминале где запущен `npm run dev`
3. Статус видео в Supabase Dashboard → Table Editor → videos

### Не приходит письмо с подтверждением

**Решение:**
1. В dev режиме Supabase показывает ссылку в консоли
2. Или используйте Mock SMTP в настройках Supabase

### Ошибка при загрузке видео

**Проверьте:**
1. Storage bucket `videos` создан
2. Storage policies применены
3. Размер видео не превышает лимит (по умолчанию 50MB)

## 📚 Дополнительные ресурсы

- [README.md](./README.md) - Полная документация
- [SETUP.md](./SETUP.md) - Детальные инструкции по настройке
- Структура базы данных в `supabase-migration.sql`

## 🚀 Готовы к деплою?

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
vercel

# Добавьте переменные окружения в Vercel Dashboard:
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_ROLE_KEY
# - REPLICATE_API_TOKEN
```


