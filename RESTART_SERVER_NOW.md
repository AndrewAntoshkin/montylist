# ⚠️ НУЖЕН ПЕРЕЗАПУСК СЕРВЕРА

## Что случилось?

Внесены изменения в код, но Next.js dev server использует старую скомпилированную версию.

## ✅ Решение (30 секунд)

### Шаг 1: Остановите сервер

В терминале где запущен `npm run dev`:
```
Нажмите: Ctrl+C
```

### Шаг 2: Запустите снова

```bash
npm run dev
```

### Шаг 3: Подождите загрузки

Дождитесь сообщения:
```
✓ Ready in X.Xs
```

---

## 🎯 После перезапуска

Система будет работать в **Gemini-only режиме** (как раньше).

FFmpeg scene detection **ВЫКЛЮЧЕН** по умолчанию.

**Чтобы включить FFmpeg:**

1. Создайте/откройте `.env.local`
2. Добавьте:
   ```bash
   USE_SCENE_DETECTION=true
   SCENE_DETECTION_THRESHOLD=0.3
   ```
3. Перезапустите сервер еще раз

---

## 🧪 Тестирование

**Сначала протестируйте скрипт:**
```bash
node scripts/test-scene-detection.js /path/to/video.mp4
```

Если тест успешен → включайте `USE_SCENE_DETECTION=true`

---

**Перезапустите сервер сейчас!** ⚡












