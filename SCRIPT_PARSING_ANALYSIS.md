# 📝 Анализ парсинга сценариев

## Текущая реализация

### Форматы поддержки
- ✅ DOCX (mammoth)
- ✅ DOC (word-extractor)
- ✅ TXT

### Парсинг имён персонажей

**Текущий паттерн:**
```typescript
CHARACTER_NAME_PATTERN = /^([А-ЯЁA-Z][А-ЯЁA-Z\s\-]{1,30})(\s*\(.*?\))?\s*$/
```

**Проблемы:**
1. ⚠️ Требует ВСЕ заглавные буквы - не работает с "Галина" (только "ГАЛИНА")
2. ⚠️ Ограничение длины 30 символов
3. ⚠️ Не учитывает разные форматы (таблицы, списки, форматирование)

### Парсинг описаний

**Текущая реализация:**
- Извлекает описания только из ремарок в скобках после имени
- `extractDescription(remark)` - только из `(ремарка)`
- Не ищет описания в других местах

**Проблемы:**
1. ⚠️ Описания могут быть в отдельной строке после имени
2. ⚠️ Описания могут быть в таблице
3. ⚠️ Описания могут быть в начале сценария (список персонажей)

---

## Проблемы с разными форматами

### Формат 1: Классический (всё заглавными)
```
ГАЛИНА
Проходите. Присаживайтесь
```
✅ Работает

### Формат 2: С ремарками
```
ГАЛИНА (за кадром)
Проходите.
```
✅ Работает

### Формат 3: Смешанный регистр
```
Галина
Проходите.
```
❌ НЕ работает (требует все заглавные)

### Формат 4: Таблица
```
| Персонаж | Описание | Реплика |
| ГАЛИНА | Женщина 30 лет | Проходите |
```
❌ НЕ работает (не парсит таблицы)

### Формат 5: Список персонажей в начале
```
ПЕРСОНАЖИ:
Галина - женщина 30 лет, менеджер салона
Валя - девушка 25 лет, стилист
```
❌ НЕ работает (не ищет описания в начале)

### Формат 6: Описание в отдельной строке
```
ГАЛИНА
(Женщина 30 лет, менеджер салона)
Проходите.
```
⚠️ Частично работает (только если в скобках на той же строке)

---

## Рекомендации по улучшению

### 1. Гибкий паттерн имён

**Текущий:**
```typescript
/^([А-ЯЁA-Z][А-ЯЁA-Z\s\-]{1,30})(\s*\(.*?\))?\s*$/
```

**Улучшенный:**
```typescript
// Вариант 1: Имя в начале строки (заглавные или смешанный регистр)
/^([А-ЯЁA-Z][А-ЯЁA-Zа-яё\s\-]{1,30})(\s*\(.*?\))?\s*$/i

// Вариант 2: Имя может быть в любом регистре, но должно быть отдельной строкой
/^([А-ЯЁA-Z][А-ЯЁа-яё\s\-]{1,50})(\s*[:\-]?\s*(\(.*?\))?)?\s*$/i
```

### 2. Поиск описаний в разных местах

**Текущий:**
- Только из ремарок после имени

**Улучшенный:**
1. Ремарки после имени: `ГАЛИНА (женщина 30 лет)`
2. Отдельная строка после имени: 
   ```
   ГАЛИНА
   (Женщина 30 лет)
   ```
3. Список персонажей в начале сценария:
   ```
   ПЕРСОНАЖИ:
   Галина - женщина 30 лет
   ```
4. Таблицы (если есть структурированный формат)

### 3. Семантический анализ

**Текущий:**
- Простой паттерн-матчинг

**Улучшенный:**
1. Анализ контекста (что идёт после имени)
2. Определение типа строки (имя, реплика, описание, ремарка)
3. Учёт форматирования (жирный, курсив = имя персонажа)

---

## План улучшений

### Приоритет 1: Гибкий паттерн имён
- Разрешить смешанный регистр
- Увеличить лимит длины
- Учитывать разные форматы (с двоеточием, тире)

### Приоритет 2: Поиск описаний
- Искать описания в отдельной строке после имени
- Искать список персонажей в начале сценария
- Извлекать описания из разных мест

### Приоритет 3: Семантический анализ
- Определять тип строки по контексту
- Учитывать форматирование (если доступно)
- Анализировать структуру сценария

---

## Дата создания
2025-01-15
