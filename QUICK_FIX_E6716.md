# 🚀 Быстрое исправление E6716

## Что было сделано:

### ✅ Исправлено #1: Moov atom error
- Изменили FFmpeg кодировку: `-c:a aac` вместо `-c:a copy`
- Добавили `-movflags +faststart` для правильного MP4

### ✅ Исправлено #2: E6716 error
- Добавили умный выбор URL: сначала public (короче), потом signed
- Улучшили обработку ошибок Replicate

## 🎯 Что нужно сделать СЕЙЧАС:

### Сделать bucket `videos` публичным

Это **критически важно** для исправления E6716!

**В Supabase Dashboard:**
1. Откройте https://supabase.com/dashboard
2. Ваш проект → **Storage** → **videos**
3. Шестеренка (настройки) → **Public bucket** ✅
4. Сохранить

**Почему это безопасно:**
- Chunks имеют уникальные имена (`chunk_0_1763388618841.mp4`)
- Их невозможно угадать
- После обработки автоматически удаляются
- **Public URLs работают надёжнее** для Replicate

## 🧪 Тест:

1. Сделайте bucket публичным (см. выше)
2. Загрузите длинное видео (>20 мин)
3. Выберите "Длинное видео"
4. Смотрите логи - должно быть:
   ```
   ✅ Using public URL for chunk 0
   ✅ Using public URL for chunk 1
   ✅ Using public URL for chunk 2
   ```
5. Chunks должны обработаться без E6716!

## 📊 Если не помогло:

1. Проверьте [status.replicate.com](https://status.replicate.com/) - может быть временная проблема
2. Попробуйте другое видео
3. Проверьте формат видео (должен быть MP4/MOV)

---

**Важно:** Chunked processing теперь работает как раньше:
- ✅ Разрезаем видео на 20-минутные куски
- ✅ FFmpeg создаёт правильные MP4 chunks
- ✅ Загружаем в Storage
- ✅ Получаем public URL (короткий и надёжный)
- ✅ Replicate обрабатывает каждый chunk
- ✅ Склеиваем результаты в общую таблицу

