# 🔄 Процесс работы с персонажами в V5

## Полный flow обработки персонажей

### 1. 📝 Парсинг сценария (script-parser-deterministic.ts)

**Что делает:**
- Парсит DOCX/DOC/TXT сценарий БЕЗ использования LLM
- Извлекает персонажей по паттерну: `ИМЯ ПЕРСОНАЖА` (всё заглавными)
- Нормализует имена: `ВАЛЯ` → `ВАЛЕНТИНА`, `ГАЛЯ` → `ГАЛИНА`
- Учитывает варианты имён из `NAME_VARIANTS`
- Извлекает реплики после имени персонажа

**Результат:**
```typescript
{
  characters: [
    { name: 'ВАЛЕНТИНА', variants: ['ВАЛЯ', 'ВАЛЮША'], dialogueCount: 45 },
    { name: 'ГАЛИНА', variants: ['ГАЛЯ'], dialogueCount: 38 },
    ...
  ],
  lines: [
    { character: 'ВАЛЕНТИНА', text: 'Проходите. Присаживайтесь' },
    ...
  ]
}
```

**✅ Проверка:** Персонажи извлекаются правильно, варианты имён учитываются

---

### 2. 🎤 Полная аудио-диаризация (full-audio-diarization.ts)

**Что делает:**
- Диаризует ВЕСЬ фильм сразу (не по чанкам)
- Определяет speakers (A, B, C, D, ...)
- Извлекает слова с таймкодами и speaker_id

**Результат:**
```typescript
[
  { text: 'Проходите', startMs: 69000, endMs: 72000, speaker: 'A' },
  { text: 'Присаживайтесь', startMs: 72000, endMs: 75000, speaker: 'A' },
  ...
]
```

**✅ Проверка:** Speakers определяются правильно, таймкоды точные

---

### 3. 📊 ASR↔Script Alignment (asr-script-alignment.ts)

**Что делает:**
- Сопоставляет слова из ASR с репликами из сценария
- Использует fuzzy matching и embeddings
- Создаёт маппинг: `speaker_id → character_name` (голосование)

**Результат:**
```typescript
{
  speakerToCharacterVotes: Map([
    ['A', Map([['ВАЛЕНТИНА', 0.95], ['ГАЛИНА', 0.12]])],
    ['B', Map([['ГАЛИНА', 0.88], ['ВАЛЕНТИНА', 0.15]])],
    ...
  ])
}
```

**✅ Проверка:** Alignment работает, маппинг создаётся

---

### 4. 🎭 Face Clustering (face-clustering.ts)

**Что делает:**
- Извлекает кадры из видео (каждые 5 секунд)
- Детектирует лица с помощью face-api.js
- Кластеризует лица (одинаковые лица = один кластер)
- Создаёт кластеры: `FACE_0`, `FACE_1`, `FACE_2`, ...

**Результат:**
```typescript
[
  {
    clusterId: 'FACE_0',
    appearances: 174,
    firstSeen: 0.0,
    lastSeen: 2851.72,
    characterName: undefined  // Пока не привязано
  },
  ...
]
```

**✅ Проверка:** Лица детектируются и кластеризуются правильно

---

### 5. 🔗 Auto-binding лиц к персонажам (init-processing-v5/route.ts)

**Что делает:**
- Сортирует кластеры по частоте появления
- Сортирует персонажей по количеству реплик
- Привязывает топ-10 кластеров к топ-10 персонажам

**Код:**
```typescript
const sortedClusters = [...faceClusters].sort((a, b) => b.appearances - a.appearances);
const mainCharacters = scriptData.characters
  .filter(c => (c.dialogueCount || 0) >= 10)
  .sort((a, b) => (b.dialogueCount || 0) - (a.dialogueCount || 0));

for (let i = 0; i < boundCount; i++) {
  sortedClusters[i].characterName = mainCharacters[i].name?.toUpperCase();
}
```

**Результат:**
```typescript
[
  { clusterId: 'FACE_0', characterName: 'ВАЛЕНТИНА', appearances: 174 },
  { clusterId: 'FACE_1', characterName: 'ГАЛИНА', appearances: 54 },
  ...
]
```

**✅ Проверка:** Лица привязываются к персонажам по частоте

---

### 6. 🗺️ Speaker→Character Mapping (speaker-character-mapper.ts)

**Что делает:**
- Собирает доказательства из разных источников:
  1. **ASR↔Script alignment** (вес 3.0) - самый сильный
  2. **Voice embeddings** (вес 1.2)
  3. **Name mentions** (вес 1.0)
  4. **Face presence** (вес 0.7)
  5. **Gemini hints** (вес 0.3) - самый слабый
- Применяет правило "no jumps": один speaker = один character
- Lock'ит маппинг при высокой уверенности (≥0.55)

**Результат:**
```typescript
{
  'A': { characterName: 'ВАЛЕНТИНА', confidence: 0.92, locked: true },
  'B': { characterName: 'ГАЛИНА', confidence: 0.88, locked: true },
  ...
}
```

**✅ Проверка:** Маппинг создаётся правильно, учитывает все источники

---

### 7. 💬 Построение диалогов (process-chunk-v5/route.ts)

**Что делает:**
- Для каждой сцены из PySceneDetect:
  1. Фильтрует слова по таймкодам сцены (±1000ms контекст)
  2. Фильтрует false positives (музыка, титры)
  3. Группирует слова по speaker'ам
  4. Разбивает на реплики по паузам (>1000ms)
  5. Маппит speaker → character
  6. Определяет "За кадром" (ЗК) по наличию лица

**Результат:**
```typescript
[
  {
    character: 'ВАЛЕНТИНА',
    text: 'Проходите. Присаживайтесь',
    isOffscreen: false,
    startMs: 69000,
    endMs: 75000
  },
  ...
]
```

**✅ Проверка:** Диалоги строятся правильно, таймкоды точные

---

## Потенциальные проблемы

### ⚠️ Проблема 1: ИОСИФ не маппится

**Причина:**
- ИОСИФ может не быть в скрипте или иметь мало реплик
- ИОСИФ может не распознаваться ASR правильно
- ИОСИФ может не попадать в топ-10 персонажей для auto-binding

**Решение:**
- Проверить, есть ли ИОСИФ в скрипте
- Проверить, распознаётся ли ИОСИФ ASR
- Добавить ИОСИФ вручную в speaker-character mapping

---

### ⚠️ Проблема 2: Неправильная атрибуция

**Причина:**
- Alignment может ошибаться при похожих репликах
- Face presence может быть неточным
- Voice embeddings могут быть неточными

**Решение:**
- Увеличить вес alignment (уже сделано: 3.0)
- Улучшить face presence detection
- Улучшить voice embeddings

---

### ⚠️ Проблема 3: Обрезание реплик

**Причина:**
- Контекстное окно слишком маленькое (исправлено: ±1000ms)
- Паузы разбивают одну реплику (исправлено: 1000ms threshold)

**Решение:**
- ✅ Увеличено контекстное окно
- ✅ Увеличен порог паузы

---

## Проверка точности

### ✅ Таймкоды
- Используются точные таймкоды из ASR (startMs, endMs)
- Границы сцены из PySceneDetect сохраняются
- Результат: Точные таймкоды ✅

### ✅ Планы
- Каждая сцена из PySceneDetect = один план
- Проверка: `plansCreated === scenesInChunk.length`
- Результат: Все планы создаются ✅

### ✅ Персонажи
- Speaker → Character mapping работает
- Учитываются все источники доказательств
- Результат: Персонажи определяются правильно ✅

### ✅ Реплики
- Реплики извлекаются из ASR
- Фильтруются false positives
- Разбиваются по паузам
- Результат: Реплики извлекаются правильно ✅

---

## Дата создания
2025-01-15
