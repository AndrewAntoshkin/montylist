# 🧪 Быстрый тест chunked процесса

## Цель
Проверить что chunked процесс работает end-to-end, несмотря на проблемы с Replicate API.

---

## Вариант 1: Короткое тестовое видео (рекомендуется)

### Требования к видео:
- ✅ Длительность: **2-5 минут**
- ✅ Формат: **MP4 (H.264)**
- ✅ Размер: **<50MB**
- ✅ Разрешение: **720p или 1080p**

### Где взять тестовое видео:
```bash
# Опция 1: Записать короткое видео на телефон/камеру
# Опция 2: Скачать sample видео
# https://sample-videos.com/
# https://test-videos.co.uk/

# Опция 3: Создать из существующего (первые 5 минут)
ffmpeg -i длинное-видео.mp4 -t 300 -c copy тест-5мин.mp4
```

### Шаги теста:
1. Запустите проект: `npm run dev`
2. Откройте дашборд
3. Нажмите **"Длинное видео"** (зеленая кнопка)
4. Загрузите короткое видео (2-5 минут)
5. Нажмите "Продолжить"

### Что должно произойти:
```
✅ Skipping auto-process for video: xxx
✅ 🎬 Processing video xxx in 1 chunk(s)
   (видео <20 минут → 1 чанк)
✅ 📹 Processing chunk 1/1: 00:00:00 - 00:XX:XX
✅ Attempt 1/3 to create prediction...
```

### Ожидаемые результаты:

#### Сценарий A: Успех 🎉
```
✅ Prediction created successfully on attempt 1
✅ Chunk 1 succeeded!
✅ Video processing completed successfully
```
→ **Отлично!** Chunked процесс полностью работает!

#### Сценарий B: Retry помогли 🔄
```
❌ Attempt 1/3 failed: {error}
⏳ Waiting 2000ms before retry...
✅ Prediction created successfully on attempt 2
```
→ **Хорошо!** Retry логика работает, временные сбои обрабатываются.

#### Сценарий C: Все попытки упали ❌
```
❌ Attempt 1/3 failed: E6716
❌ Attempt 2/3 failed: E6716
❌ Attempt 3/3 failed: E6716
```
→ **Replicate API недоступен** - подождите 30 минут и повторите.

---

## Вариант 2: Проверка через обычный процесс

Если chunked процесс падает из-за Replicate, проверьте работает ли обычный процесс:

1. Нажмите **"Новый лист"** (белая кнопка)
2. Загрузите то же короткое видео
3. Если это тоже падает → проблема в Replicate в целом

---

## Вариант 3: Мониторинг логов

### Запустите проект с подробными логами:
```bash
npm run dev
```

### Откройте консоль и следите за:
```
✅ Skipping auto-process → chunked процесс запущен
✅ GET /api/videos/xxx 200 → endpoint работает
✅ 🎬 Processing video → разбивка на чанки работает
✅ Attempt 1/3 → retry логика активна
```

---

## Чек-лист работоспособности

### Наша часть (должно работать):
- [ ] Кнопка "Длинное видео" появляется
- [ ] Модальное окно открывается
- [ ] Длительность определяется автоматически
- [ ] Upload проходит успешно (200 OK)
- [ ] Лог: "Skipping auto-process..."
- [ ] GET /api/videos/xxx возвращает 200
- [ ] Chunked endpoint запускается
- [ ] Видео разбивается на чанки корректно

### Внешняя часть (может не работать):
- [ ] Replicate prediction создается
- [ ] Prediction переходит в "succeeded"
- [ ] Output парсится

---

## Интерпретация результатов

### Если всё работает ✅
Отлично! Можно тестировать с более длинными видео.

### Если падает на prediction ❌
**Причина:** Replicate API  
**Решение:** 
1. Подождать 30 минут
2. Попробовать снова
3. Проверить https://replicate.com/status
4. Рассмотреть альтернативные модели/API

### Если ломается до prediction ⚠️
**Причина:** Наш код  
**Решение:** Проверить логи, найти ошибку, исправить

---

## Быстрая диагностика

### Команда для проверки структуры:
```bash
# Проверить что все файлы на месте
ls -la lib/video-chunking.ts
ls -la lib/replicate-helper.ts
ls -la app/api/process-video-chunked/route.ts
ls -la components/UploadModalLong.tsx
```

### Проверить базу данных:
```sql
-- Проверить последнее видео
SELECT id, original_filename, duration, status, created_at 
FROM videos 
ORDER BY created_at DESC 
LIMIT 1;

-- Проверить есть ли duration
SELECT COUNT(*) as with_duration 
FROM videos 
WHERE duration IS NOT NULL;
```

---

## Следующие шаги после теста

### Если тест успешен:
1. ✅ Протестировать с видео 25-30 минут (2 чанка)
2. ✅ Протестировать с видео 45 минут (3 чанка)
3. ✅ Убедиться что overlap работает корректно
4. ✅ Проверить дедупликацию сцен

### Если тест не успешен из-за Replicate:
1. Задокументировать ошибку
2. Проверить альтернативные API:
   - Google Gemini API напрямую
   - OpenAI GPT-4 Vision
   - Anthropic Claude 3.5 Vision
3. Рассмотреть локальные модели (Ollama + LLaVA)

---

## Полезные команды

### Очистить тестовые данные:
```sql
-- Удалить тестовые видео
DELETE FROM videos WHERE original_filename LIKE '%тест%';
DELETE FROM videos WHERE duration < 600; -- короче 10 минут
```

### Проверить логи в реальном времени:
```bash
npm run dev | grep -E '(🎬|📹|✅|❌|Attempt)'
```

### Мониторить Replicate:
```bash
# Если есть API ключ
curl -H "Authorization: Token $REPLICATE_API_TOKEN" \
  https://api.replicate.com/v1/predictions/{prediction_id}
```

---

## Контакты и поддержка

### Документация:
- `CHUNKED_VIDEO_PROCESSING.md` - полная документация
- `IMPROVEMENTS_SUMMARY.md` - сводка изменений
- `README.md` - общая информация о проекте

### Если нужна помощь:
1. Проверьте логи в консоли
2. Проверьте документацию
3. Проверьте GitHub Issues Replicate
4. Проверьте статус на replicate.com/status

---

**Удачи с тестированием! 🚀**




