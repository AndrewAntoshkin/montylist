# 🔢 Исправление нумерации планов - Быстрое руководство

## Проблема
Планы нумеруются с пропусками: **1, 2, 4, 6, 8...** вместо **1, 2, 3, 4, 5...**

## Решение

### Для ТЕКУЩЕГО видео (с пропусками)

1. Откройте страницу с таблицей монтажа
2. Нажмите кнопку **"Перенумеровать"** (справа от названия файла)
3. Подтвердите действие
4. Обновите страницу после завершения

### Для НОВЫХ видео

Пропуски больше не будут появляться - исправление применяется автоматически при обработке.

## Что было исправлено?

### 1. Автоматическая перенумерация при финализации
- Файл: `app/api/finalize-processing/route.ts`
- Теперь перенумеровывает `plan_number` вместе с `order_index`

### 2. Кнопка для ручной перенумерации
- Файл: `components/MontageTableClient.tsx`
- Добавлена кнопка "Перенумеровать" в интерфейс

### 3. API endpoint для перенумерации
- Файл: `app/api/renumber-plans/route.ts` (новый)
- POST `/api/renumber-plans` с `{ videoId: "..." }`

## Почему это происходило?

При обработке видео по частям (чанкам):
- Каждый чанк резервировал диапазон номеров планов
- Если Gemini возвращал меньше планов - возникали пропуски
- В базе сохранялись номера с пропусками

**Пример:**
```
Чанк 0: планы 1-50   (50 планов)
Чанк 1: планы 51-75  (25 планов, ожидалось 50 → пропуск 76-100)
Чанк 2: планы 101-150 (50 планов)
```

## Проверка результата

После перенумерации:
- ✅ **Было:** 1, 2, 4, 6, 8, 10, 11, 13, 15...
- ✅ **Стало:** 1, 2, 3, 4, 5, 6, 7, 8, 9...

---

**Примечание:** Перенумерация безопасна и не изменяет порядок планов, таймкоды или содержание.












