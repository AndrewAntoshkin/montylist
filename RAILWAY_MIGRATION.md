# 🚂 Миграция с Vercel на Railway

## Почему Railway?

| Функция | Vercel | Railway |
|---------|--------|---------|
| PySceneDetect | ❌ Не работает | ✅ Работает |
| FFmpeg | ❌ Не работает | ✅ Работает |
| Face Recognition | ❌ Не работает | ✅ Работает |
| Долгие задачи (>60 сек) | ❌ Timeout | ✅ Без лимита |
| Python CLI | ❌ Нет | ✅ Есть |

---

## Шаг 1: Подготовка проекта

Убедитесь, что в проекте есть:
- ✅ `nixpacks.toml` (уже создан)
- ✅ `Dockerfile` (уже создан)
- ✅ `.dockerignore` (уже создан)

---

## Шаг 2: Регистрация на Railway

1. Перейдите на https://railway.app
2. Нажмите **"Login"** → войдите через GitHub
3. Получите **$5 кредитов** на 30 дней (без карты!)

---

## Шаг 3: Создание проекта

### Вариант A: Через GitHub (рекомендуется)

1. На дашборде Railway нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите репозиторий `carete-montage`
4. Railway автоматически определит Next.js

### Вариант B: Через CLI

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите
railway login

# Создайте проект
railway init

# Свяжите с репозиторием
railway link
```

---

## Шаг 4: Настройка переменных окружения

В Railway Dashboard → **Variables** добавьте:

```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...
GOOGLE_AI_API_KEY=AIza...
ASSEMBLYAI_API_KEY=your-key
REPLICATE_API_TOKEN=r8_...
```

---

## Шаг 5: Деплой

### Автоматический деплой:
- Каждый push в `main` → автодеплой

### Ручной деплой:
```bash
railway up
```

---

## Шаг 6: Получение URL

После деплоя Railway даст URL:
```
https://carete-montage-production.up.railway.app
```

Или настройте свой домен в **Settings → Domains**.

---

## Шаг 7: Обновите Supabase

1. В Supabase → **Authentication** → **URL Configuration**
2. **Site URL**: `https://your-app.up.railway.app`
3. **Redirect URLs**: добавьте `https://your-app.up.railway.app/auth/callback`

---

## Проверка работоспособности

После деплоя проверьте:

```bash
# PySceneDetect работает?
railway run scenedetect --version

# FFmpeg работает?
railway run ffmpeg -version

# Face Recognition работает?
railway run node -e "require('@vladmandic/face-api')"
```

---

## Стоимость

| Что делает пользователь | Примерная стоимость |
|------------------------|---------------------|
| Загрузка страницы | ~$0.001 |
| Обработка 48 мин видео | ~$0.05-0.10 |
| Face Recognition | ~$0.01-0.02 |
| **В месяц (10 обработок)** | **~$1-2** |

**$5 кредитов хватит на ~50-100 обработок!**

---

## Сравнение с Vercel

| Метрика | Vercel | Railway |
|---------|--------|---------|
| Обработка видео | ❌ Невозможна | ✅ Работает |
| Стоимость (Hobby) | $20/мес | $5/мес |
| Лимит выполнения | 60 сек | Без лимита |
| Python/FFmpeg | ❌ | ✅ |

---

## Удаление Vercel (опционально)

После успешной миграции:

1. В Vercel Dashboard → Settings → General
2. Scroll вниз → **Delete Project**
3. Обновите DNS если использовали свой домен

---

## Troubleshooting

### Ошибка: "scenedetect not found"
```bash
# Проверьте nixpacks.toml
railway logs
```

### Ошибка: "canvas build failed"
```bash
# Railway использует Dockerfile вместо nixpacks
# Dockerfile уже настроен правильно
```

### Долгий билд (>10 мин)
- Первый билд долгий (установка зависимостей)
- Последующие быстрее (кэш)

---

## Готово!

Теперь ваше приложение:
- ✅ Обрабатывает видео
- ✅ Использует PySceneDetect
- ✅ Поддерживает Face Recognition
- ✅ Работает для всех пользователей
