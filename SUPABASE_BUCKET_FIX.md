# Исправление ошибки E6716 - Supabase Storage

## 🐛 Проблема

Replicate падает с ошибкой:
```
Error: Prediction failed: Director: unexpected error handling prediction (E6716)
```

## 🔍 Причина

**E6716** - внутренняя ошибка Replicate, возникает когда:
1. Replicate не может скачать видео по URL
2. Signed URL от Supabase слишком длинный/сложный
3. Bucket приватный и signed URL некорректно обрабатывается

## ✅ Решение: Сделать bucket публичным

### Вариант 1: Через Supabase Dashboard (РЕКОМЕНДУЕТСЯ)

1. Откройте https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Storage** → **videos**
4. Нажмите на шестеренку (настройки bucket)
5. Включите **Public bucket**
6. Сохраните

### Вариант 2: Создать новый публичный bucket для chunks

```sql
-- Создать отдельный публичный bucket для chunks
insert into storage.buckets (id, name, public)
values ('video-chunks', 'video-chunks', true);

-- Настроить политики доступа
create policy "Give users access to own folder"
on storage.objects for all
using (bucket_id = 'video-chunks' AND (storage.foldername(name))[1] = auth.uid()::text);
```

Потом в коде изменить:
```typescript
// Вместо 'videos' использовать 'video-chunks'
await supabase.storage.from('video-chunks').upload(...)
```

### Вариант 3: Оставить приватный bucket и использовать более короткие signed URLs

В `app/api/init-chunked-processing/route.ts` мы уже добавили:
1. Проверку публичного URL (если bucket публичный)
2. Fallback на signed URL (если приватный)

## 🧪 Как проверить

После того как сделаете bucket публичным:

1. Загрузите длинное видео
2. Проверьте логи - должно быть "Using public URL for chunk X"
3. Chunks должны обработаться без ошибки E6716

## 📝 Почему публичный bucket безопаснее

- Chunks генерируются с уникальными именами (`chunk_0_1763388618841.mp4`)
- Их невозможно угадать
- После обработки можно удалять
- Replicate гарантированно может скачать файл

---

**Дата:** 17 ноября 2025  
**Статус:** В процессе исправления




