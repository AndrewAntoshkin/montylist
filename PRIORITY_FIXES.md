# 🔧 Приоритетные исправления

## Критические проблемы из сравнения

### 1. ⚠️ Обрезание реплик (ВЫСОКИЙ ПРИОРИТЕТ)

**Проблема:**
- Реплики обрезаются в конце: "столкн..." вместо "столкнулись"
- Не хватает начала: "Меня парень" вместо "А у меня парень"

**Причины:**
1. Контекстное окно для слов слишком маленькое: `-500ms` и `+500ms`
2. Фильтрация слов может обрезать слова
3. Разбиение диалогов по паузам может обрезать реплики

**Решение:**
- Увеличить контекстное окно с `±500ms` до `±1000ms` для захвата полных реплик
- Увеличить `PAUSE_THRESHOLD_MS` с `500ms` до `1000ms` чтобы не разбивать одну реплику
- Проверить фильтрацию слов - не обрезаются ли слова при фильтрации

---

### 2. ⚠️ Неправильная атрибуция ИОСИФ (ВЫСОКИЙ ПРИОРИТЕТ)

**Проблема:**
- 00:06:08:13: "Арабы" говорит ИОСИФ, но в логах это приписано ГАЛИНА

**Причина:**
- ИОСИФ не маппится в speaker-character mapping
- Возможно, ИОСИФ не в скрипте или не распознаётся ASR

**Решение:**
- Проверить, есть ли ИОСИФ в скрипте
- Добавить ИОСИФ в speaker-character mapping
- Проверить ASR - распознаётся ли ИОСИФ

---

### 3. ⚠️ ASR ошибки (СРЕДНИЙ ПРИОРИТЕТ)

**Проблема:**
- "икедо" вместо "айкидо"
- "ожленый" вместо "жжёный"
- "Чё" вместо "Че"

**Причина:**
- Ошибки ASR (Replicate Whisper)

**Решение:**
- Это ограничение ASR, сложно исправить
- Можно добавить постобработку для частых ошибок
- Или использовать другой ASR сервис

---

### 4. ⚠️ Объединение разных реплик (СРЕДНИЙ ПРИОРИТЕТ)

**Проблема:**
- 00:15:02:14: в логах объединены две разные реплики ВАЛЯ

**Причина:**
- Разбиение диалогов не учитывает смену контекста
- Пауза между репликами недостаточна для разбиения

**Решение:**
- Улучшить логику разбиения диалогов
- Учитывать не только паузы, но и контекст сцены

---

### 5. ⚠️ Неправильная реплика (СРЕДНИЙ ПРИОРИТЕТ)

**Проблема:**
- 00:06:24:03: в логах "Тоже араб. Только он очень не...", но в реальном "Так он очень"

**Причина:**
- Неправильное разбиение диалогов
- Возможно, слова из разных реплик объединены

**Решение:**
- Проверить разбиение диалогов
- Убедиться, что слова не объединяются из разных реплик

---

## План исправлений

### Этап 1: Критические исправления (сейчас)

1. ✅ Увеличить контекстное окно для слов: `±500ms` → `±1000ms`
2. ✅ Увеличить `PAUSE_THRESHOLD_MS`: `500ms` → `1000ms`
3. ✅ Проверить фильтрацию слов - не обрезаются ли слова

### Этап 2: Атрибуция говорящих

4. ⏳ Проверить, есть ли ИОСИФ в скрипте
5. ⏳ Добавить ИОСИФ в speaker-character mapping
6. ⏳ Проверить ASR - распознаётся ли ИОСИФ

### Этап 3: Улучшение разбиения

7. ⏳ Улучшить логику разбиения диалогов
8. ⏳ Учитывать контекст сцены при разбиении

---

## Ожидаемый результат

После исправлений:
- ✅ Реплики не обрезаются (полные фразы)
- ✅ ИОСИФ правильно атрибутирован
- ✅ Меньше ошибок разбиения диалогов
- ⚠️ ASR ошибки останутся (это ограничение ASR)

---

## Дата создания
2025-01-15
