# 📋 Загрузка сценария — Новая функция

## Обзор

Теперь при создании монтажного листа можно **сначала загрузить сценарий** (DOCX или TXT), чтобы система точно знала имена и характеристики персонажей.

## Как это работает

### Пайплайн обработки:

```
1. 📝 ЗАГРУЗКА СЦЕНАРИЯ (опционально)
   └── Парсинг → Извлечение персонажей с именами и полом
   
2. 📹 ЗАГРУЗКА ВИДЕО
   └── TUS upload → Supabase Storage
   
3. ✂️ FFmpeg
   └── Определение таймкодов монтажных склеек
   
4. 🤖 Gemini AI
   └── Анализ видео С КОНТЕКСТОМ персонажей из сценария
   
5. 🔗 POST-PROCESSING
   └── Замена generic имён (ЖЕНЩИНА, МУЖЧИНА) на реальные
```

## Что изменилось в UI

### UploadModal теперь двухшаговый:

**Шаг 1: Сценарий**
- Загрузите .docx или .txt файл
- Система извлечёт персонажей автоматически
- Покажет найденных главных персонажей
- Можно пропустить этот шаг

**Шаг 2: Видео**
- Загрузите видео как обычно
- Персонажи из сценария будут использованы при анализе

## Форматы сценария

### Поддерживаемые форматы:
- **.docx** — Microsoft Word (рекомендуется)
- **.txt** — Текстовый файл (UTF-8)

### Что парсер извлекает:
1. **Имена персонажей** из диалогов
2. **Пол персонажа** (по имени)
3. **Количество реплик** (для определения главных/второстепенных)
4. **Варианты имени** (ГАЛЯ, ГАЛИНА, ГАЛОЧКА → ГАЛЯ)

### Пример сценария:

```
СЦЕНА 1. ИНТ. САЛОН КРАСОТЫ - ДЕНЬ

ТОМА
Проходите, присаживайтесь.

ГАЛЯ
Спасибо. Я записана на маникюр.

ТОМА
Да, вижу вас в списке. Сейчас вас примут.
```

## Что это даёт

### Без сценария:
```
ЖЕНЩИНА
Проходите, присаживайтесь.

ДЕВУШКА
Спасибо.
```

### Со сценарием:
```
ТОМА
Проходите, присаживайтесь.

ГАЛЯ
Спасибо.
```

## Технические детали

### Новые файлы:
- `lib/script-parser.ts` — парсинг DOCX/TXT
- `lib/script-characters.ts` — работа с персонажами
- `lib/script-video-merger.ts` — мердж данных
- `app/api/parse-script/route.ts` — API для парсинга

### Модифицированные файлы:
- `components/UploadModal.tsx` — двухшаговый UI
- `lib/gemini-prompt.ts` — промпт с персонажами
- `app/api/complete-upload/route.ts` — передача scriptData
- `app/api/init-chunked-processing/route.ts` — сохранение scriptData
- `app/api/process-chunk/route.ts` — использование персонажей
- `app/api/process-video/route.ts` — пост-обработка

### Типы:
```typescript
interface ScriptData {
  title?: string;
  characters: ScriptCharacterInfo[];
  sceneCount: number;
  format: 'standard' | 'freeform';
  uploadedAt: string;
}

interface ScriptCharacterInfo {
  name: string;
  normalizedName: string;
  dialogueCount: number;
  gender?: 'male' | 'female' | 'unknown';
  variants: string[];
}
```

## Как тестировать

1. Открой `http://localhost:3004`
2. Нажми "Новый лист"
3. На первом шаге загрузи сценарий (или пропусти)
4. Проверь, что персонажи отображаются
5. Загрузи видео
6. Проверь результат — имена должны быть правильные!

## Известные ограничения

- Только UTF-8 для TXT файлов
- Парсер лучше работает со стандартным форматом сценария
- Для свободного формата извлекает только спикеров из диалогов

