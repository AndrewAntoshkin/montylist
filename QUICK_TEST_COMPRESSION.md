# 🚀 Быстрый тест сжатия видео

## ✅ Готово к тестированию!

### Что изменилось:

1. **Добавлена синяя кнопка "Тестовая загрузка"** рядом с "Новый лист"
2. **Оригинальная кнопка НЕ ТРОНУТА** и работает как прежде!
3. **Сжатие работает в браузере** через FFmpeg.wasm (как на redpandacompress.com)

---

## 🎯 Быстрый старт

### 1. Откройте приложение

```
http://localhost:3000
```

Dev сервер уже запущен! ✅

### 2. Залогиньтесь

Войдите в систему и перейдите в Dashboard.

### 3. Найдите две кнопки

Справа вверху вы увидите:
- 🔵 **"Тестовая загрузка"** ← НОВАЯ с сжатием
- ⚪ **"Новый лист"** ← СТАРАЯ работающая

### 4. Нажмите синюю кнопку

Откроется новый модал с надписью "Тестовая загрузка (с сжатием)".

Подождите ~30 секунд пока загрузится модуль FFmpeg:
- ⏳ "Загрузка модуля сжатия..."
- ✅ "Модуль сжатия готов"

### 5. Загрузите тестовое видео

**Для первого теста:**
- Небольшое видео (100-500 МБ)
- Формат MP4
- Длительность 5-10 минут

### 6. Нажмите "Начать"

**Что произойдет:**

1. **Сжатие** (2-5 минут)
   - Показывает прогресс 0-100%
   - "Сжатие видео..." 
   - Работает прямо в браузере!

2. **Загрузка** (~1 минута)
   - Сжатое видео (~180 МБ) загружается на сервер

3. **Обработка AI**
   - Автоматически запускается
   - Видео появится во вкладке "В работе"

---

## 📊 Что смотреть в консоли

Откройте DevTools (F12 или Cmd+Option+I):

```javascript
✅ FFmpeg.wasm loaded successfully
📊 Original size: 450.23MB
🎯 Target: 180MB
🗜️  Starting compression...
✅ Compressed size: 178.45MB
```

---

## ⚙️ Как это работает (техническое)

### Browser-side сжатие (как RedPanda):

```
1. FFmpeg.wasm загружается в браузер (~30 МБ)
2. Видео сжимается локально на компьютере пользователя
3. Сжатое видео (~180 МБ) загружается на сервер
4. Дальше идет обычная AI обработка
```

**Плюсы:**
- ✅ Работает на Vercel (не нужен server-side FFmpeg)
- ✅ Не нагружает сервер
- ✅ Приватность (видео не уходит до сжатия)
- ✅ Масштабируемость (каждый пользователь = свой CPU)

**Минусы:**
- ⏱️ Зависит от мощности компьютера пользователя
- 💾 Требует памяти браузера
- 🐌 Первая загрузка FFmpeg.wasm занимает время

---

## 🎛️ Настройки сжатия

В файле `components/UploadModalCompress.tsx`:

```typescript
const targetSizeMB = 180;  // Целевой размер
'-crf', '28',              // Качество (23-28)
'-vf', 'scale=-2:720',     // Разрешение (720p)
'-preset', 'medium',       // Скорость/качество
```

Можно менять под свои нужды!

---

## 🔥 Альтернативы

### Вариант A: Server-side сжатие (если browser-based медленный)

Можно сделать сжатие на сервере (Railway/VPS):
- Быстрее
- Единообразное качество
- Но не работает на Vercel Hobby

### Вариант B: Гибрид

- Легкие видео (<500 МБ) → без сжатия
- Средние (500 МБ - 1 ГБ) → browser сжатие
- Тяжелые (>1 ГБ) → server сжатие

---

## 📝 TODO после теста

После успешного тестирования решить:

1. **Оставить обе кнопки?**
   - Пользователь выбирает сам
   - "Быстрая" vs "С сжатием"

2. **Заменить старую на новую?**
   - Всегда сжимать большие файлы
   - Автоматически (если >300 МБ)

3. **Добавить выбор качества?**
   - Низкое (~100 МБ, быстро)
   - Среднее (~180 МБ, баланс)
   - Высокое (~300 МБ, лучше качество)

---

## 🐛 Если что-то не работает

1. **FFmpeg не загружается:**
   - Перезапустите dev сервер
   - Очистите кеш браузера (Cmd+Shift+R)
   - Используйте Chrome (лучшая поддержка)

2. **Сжатие зависло:**
   - Проверьте консоль на ошибки
   - Попробуйте файл меньшего размера
   - Попробуйте другой формат (MP4)

3. **Браузер зависает:**
   - Закройте другие вкладки
   - Используйте файл <1 ГБ

---

## 📖 Полная документация

Смотрите `COMPRESSION_TEST_GUIDE.md` для детальной информации.

---

**Важно:** Оригинальная функция "Новый лист" работает точно так же как раньше! 🎉




