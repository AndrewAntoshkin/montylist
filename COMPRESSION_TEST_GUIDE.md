# 🗜️ Инструкция по тестированию сжатия видео

## ✅ Что сделано

1. ✅ Добавлена кнопка **"Тестовая загрузка"** (синяя) рядом с "Новый лист"
2. ✅ Создан компонент `UploadModalCompress.tsx` со сжатием в браузере
3. ✅ Установлен пакет `@ffmpeg/ffmpeg` (WebAssembly FFmpeg)
4. ✅ Настроены заголовки для SharedArrayBuffer в `next.config.ts`
5. ✅ Оригинальная кнопка "Новый лист" НЕ ЗАТРОНУТА и работает как прежде

## 🚀 Как протестировать

### Шаг 1: Запустите dev сервер

```bash
cd /Users/andrewaitken/carete-montage
npm run dev
```

### Шаг 2: Откройте приложение

Откройте в браузере: http://localhost:3000

**Важно:** Используйте Chrome или Edge (лучшая поддержка WebAssembly)

### Шаг 3: Войдите и перейдите в Dashboard

Залогиньтесь и перейдите на страницу с монтажными листами.

### Шаг 4: Найдите синюю кнопку

Вы увидите **ДВЕ кнопки** справа вверху:
- 🔵 **"Тестовая загрузка"** (синяя) - НОВАЯ с сжатием
- ⚪ **"Новый лист"** (белая) - СТАРАЯ работающая

### Шаг 5: Нажмите "Тестовая загрузка"

Откроется модальное окно с надписью:
- "Тестовая загрузка (с сжатием)"
- "✅ Модуль сжатия готов" (после загрузки FFmpeg.wasm, ~30 секунд)

### Шаг 6: Загрузите видео

**Для теста используйте:**
- Небольшое видео (100-500 МБ)
- Лучше MP4 формат
- Длительность 5-20 минут идеально

**Что произойдет:**

1. **Загрузка FFmpeg.wasm** (~5-10 секунд)
   - Модуль загружается один раз при открытии модала
   - Размер ~30 МБ
   
2. **Выбор файла**
   - Перетащите или выберите видео
   - Отобразится имя, размер, длительность

3. **Сжатие** (кнопка "Начать")
   - Показывает прогресс 0-100%
   - "Сжатие видео..." + процент
   - Работает прямо в браузере!
   - Может занять 2-10 минут (зависит от мощности компьютера)

4. **Загрузка на сервер**
   - После сжатия автоматически загружает
   - Показывает прогресс загрузки
   
5. **Обработка AI**
   - Запускается chunked processing
   - Модал закрывается
   - Видео появляется во вкладке "В работе"

## 📊 Что наблюдать

### В консоли браузера (F12):

```
🔧 Loading FFmpeg.wasm...
✅ FFmpeg.wasm loaded successfully
📝 Writing input file to FFmpeg virtual FS...
📊 Original size: 450.23MB
🎯 Target: 180MB, Duration: 720s, Bitrate: 1895k
🗜️  Starting compression...
FFmpeg: [различные логи FFmpeg]
📖 Reading compressed file...
✅ Compressed size: 178.45MB
📤 Uploading compressed file...
✅ Upload complete! Video ID: xxx
```

### В модальном окне:

- Статус: "Загрузка модуля сжатия..." → "✅ Модуль сжатия готов"
- При сжатии: "Сжатие видео..." + прогресс бар
- "Сжатие происходит в вашем браузере"
- Статистика: "Оригинал: 450.2MB → Сжато: 178.4MB"

## 🎯 Параметры сжатия

Текущие настройки (в `UploadModalCompress.tsx`):

```typescript
Target size: 180 MB
Video codec: H.264 (libx264)
Resolution: 720p (уменьшено с оригинального)
Video bitrate: Calculated dynamically
CRF: 28 (compression quality)
Audio: AAC 128kbps
Preset: medium (баланс скорость/качество)
```

## ⚠️ Известные ограничения

### 1. Скорость
- Зависит от мощности компьютера пользователя
- На MacBook Pro (M1/M2): ~2-5 минут для 10 минут видео
- На слабых компьютерах: может быть медленнее

### 2. Память браузера
- FFmpeg работает в памяти браузера
- Для очень больших файлов (>2 ГБ) может не хватить памяти
- Рекомендуется тестировать на файлах <1.5 ГБ

### 3. Первая загрузка
- При первом открытии модала загружается FFmpeg.wasm (~30 МБ)
- Потом кешируется в браузере
- При повторном использовании загрузки нет

### 4. Совместимость
- **Chrome/Edge**: ✅ Отлично работает
- **Firefox**: ✅ Работает, но медленнее
- **Safari**: ⚠️ Могут быть проблемы с SharedArrayBuffer

## 🔍 Troubleshooting

### Проблема: "Модуль сжатия не загружается"

**Причина:** SharedArrayBuffer не доступен

**Решение:**
1. Проверьте что в `next.config.ts` есть headers
2. Перезапустите dev сервер: `npm run dev`
3. Очистите кеш браузера (Cmd+Shift+R)
4. Используйте Chrome вместо Safari

### Проблема: "Сжатие зависло на 0%"

**Причина:** FFmpeg не может обработать файл

**Решение:**
1. Попробуйте другой видео файл (MP4 рекомендуется)
2. Проверьте консоль на ошибки
3. Уменьшите размер входного файла

### Проблема: Браузер зависает

**Причина:** Недостаточно памяти

**Решение:**
1. Закройте другие вкладки
2. Используйте файл меньшего размера (<1 ГБ)
3. Увеличьте память для браузера в настройках системы

### Проблема: Низкое качество после сжатия

**Решение:** Отредактируйте параметры в `UploadModalCompress.tsx`:

```typescript
// Увеличьте target size:
const targetSizeMB = 250; // вместо 180

// Или улучшите качество:
'-crf', '23',  // вместо '28' (меньше = лучше качество)

// Или оставьте 1080p:
'-vf', 'scale=-2:1080',  // вместо 720
```

## 📈 Следующие шаги

После успешного теста:

1. **Интеграция в основной флоу**
   - Можно заменить старую кнопку на новую
   - Или оставить выбор пользователю

2. **Улучшения UI**
   - Добавить выбор качества сжатия
   - Показать предварительный размер
   - Добавить отмену сжатия

3. **Оптимизация**
   - Предзагружать FFmpeg.wasm заранее
   - Добавить воркеры для не блокировки UI
   - Кешировать FFmpeg между использованиями

4. **Альтернатива: Server-side**
   - Если browser-based медленный
   - Можно сделать серверное сжатие на Railway/VPS
   - Использовать очередь обработки

## 🎉 Преимущества browser-based подхода

✅ **Работает на Vercel** (даже Hobby plan)
✅ **Не нагружает сервер** (экономия ресурсов)
✅ **Приватность** (видео не покидает браузер до сжатия)
✅ **Масштабируемость** (каждый пользователь использует свой CPU)

## ❓ Вопросы?

Если возникнут проблемы при тестировании - дайте знать!

---

**Важно:** Оригинальная кнопка "Новый лист" работает как прежде и не затронута изменениями!




