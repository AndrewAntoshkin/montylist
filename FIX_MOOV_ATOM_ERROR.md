# Исправление ошибок chunked processing

## 🐛 Проблема #1: "moov atom not found"

При загрузке длинного видео (>20 минут) обработка падала с ошибкой:
```
Error: Prediction failed: Failed to get video duration: moov atom not found
/tmp/cog-input-2042590197.mp4: Invalid data found when processing input
```

## 🐛 Проблема #2: E6716 - "Director: unexpected error"

После исправления moov atom, появилась новая ошибка:
```
Error: Prediction failed: Director: unexpected error handling prediction (E6716)
```

## 🔍 Причина

Было **ТРИ критические проблемы**:

### 1. Использовались публичные URL вместо signed URL ❌
В `app/api/init-chunked-processing/route.ts` использовался `getPublicUrl()`, но:
- Бакет `videos` в Supabase Storage приватный
- Replicate не мог скачать файл по публичной ссылке
- Получался битый/неполный файл

### 2. Аудио кодировалось с `-c:a copy` ❌
В `lib/video-splitter.ts` использовалось копирование аудио потока:
- При разрезке видео это могло приводить к рассинхронизации
- Некоторые форматы аудио создавали проблемы с moov atom
- MP4 контейнер получался некорректным

### 3. E6716 - Replicate не мог скачать видео ❌
Даже после исправления moov atom и signed URLs:
- Signed URLs от Supabase содержат длинный JWT токен
- Replicate API может некорректно обрабатывать такие URL
- Приватный bucket создаёт дополнительные проблемы

## ✅ Решение

### 1. Изменили public URL на signed URL
```typescript
// Было:
const { data: urlData } = supabase.storage
  .from('videos')
  .getPublicUrl(chunkStoragePath);

// Стало:
const { data: urlData, error: urlError } = await supabase.storage
  .from('videos')
  .createSignedUrl(chunkStoragePath, 60 * 60 * 24 * 7); // 7 days
```

**Почему 7 дней?**
- Достаточно времени для обработки даже очень длинных видео
- Безопасно - URL автоматически истекает
- Replicate гарантированно может скачать файл

### 2. Улучшили FFmpeg кодировку
```typescript
// Было:
'-c:a copy',         // Просто копировали аудио
'-movflags +faststart',

// Стало:
'-c:a aac',             // Re-encode audio to AAC
'-b:a 128k',            // Audio bitrate
'-ar 44100',            // Sample rate
'-movflags +faststart', // moov atom в начало (CRITICAL!)
'-f mp4',               // Force MP4 format
```

**Что изменилось:**
- ✅ Аудио теперь **перекодируется в AAC** (стандарт для MP4)
- ✅ Фиксированный bitrate и sample rate
- ✅ Принудительный формат MP4
- ✅ `-movflags +faststart` гарантирует, что moov atom в начале файла

### 3. Умная система выбора URL (Public vs Signed)
```typescript
// Сначала пробуем public URL (если bucket публичный)
const { data: publicUrlData } = supabase.storage
  .from('videos')
  .getPublicUrl(chunkStoragePath);

// Тестируем, работает ли public URL
const testResponse = await fetch(publicUrlData.publicUrl, { method: 'HEAD' });
if (testResponse.ok) {
  // Используем public URL (короче и надёжнее для Replicate)
  storageUrl = publicUrlData.publicUrl;
} else {
  // Fallback на signed URL
  storageUrl = signedUrlData.signedUrl;
}
```

**Почему это важно:**
- ✅ **Public URLs короче** - нет длинного JWT токена
- ✅ **Replicate лучше их обрабатывает** - меньше ошибок E6716
- ✅ **Автоматический fallback** - если bucket приватный, используем signed URL

### 4. Улучшенная обработка ошибок
- Детектим E6716 ошибки и логируем подробности
- Retry для временных сетевых ошибок
- Понятные сообщения об ошибках

## 🎯 Результат

Теперь при загрузке длинного видео:
1. ✅ Видео успешно разрезается на chunks с корректными метаданными MP4
2. ✅ Каждый chunk загружается в Supabase Storage
3. ✅ Система автоматически выбирает лучший тип URL (public или signed)
4. ✅ Replicate успешно скачивает и обрабатывает каждый chunk
5. ✅ AI анализ проходит без ошибок
6. ✅ Результаты объединяются в общую таблицу

## 📝 Измененные файлы

1. `app/api/init-chunked-processing/route.ts` - умный выбор URL (public/signed)
2. `lib/video-splitter.ts` - правильная FFmpeg кодировка с AAC audio
3. `lib/replicate-helper.ts` - улучшенная обработка ошибок E6716

## 🧪 Как проверить

### Шаг 1: Настроить Supabase bucket (ВАЖНО!)

**Рекомендуется сделать bucket публичным для лучшей работы:**

1. Откройте https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Storage** → **videos**
4. Нажмите на шестеренку (настройки bucket)
5. Включите **Public bucket**
6. Сохраните

### Шаг 2: Протестировать загрузку

1. Загрузите видео длиннее 20 минут
2. Выберите "Длинное видео" в модалке загрузки
3. Наблюдайте в консоли:
   - FFmpeg разрезает видео на chunks
   - Chunks загружаются в Storage
   - Система выбирает "Using public URL" (если bucket публичный)
4. Дождитесь завершения обработки
5. Проверьте, что все chunks обработались успешно (статус "completed")
6. Экспортируйте монтажный лист

### Шаг 3: Если остаётся E6716

Если ошибка E6716 всё ещё появляется:
1. Убедитесь, что bucket публичный
2. Проверьте логи - должно быть "Using public URL for chunk X"
3. Попробуйте другое видео (может быть проблема с конкретным файлом)
4. Проверьте [статус Replicate](https://status.replicate.com/)

---

**Дата:** 17 ноября 2025  
**Статус:** ✅ Исправлено (требуется публичный bucket для chunks)

